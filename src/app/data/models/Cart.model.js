export class Cart {
    constructor(productName, productPrice, productImage, amount) {
        this.productName = productName
        this.productPrice = productPrice
        this.productImage = productImage
        this.amount = amount
    }
}