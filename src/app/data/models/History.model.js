export class HistoryModel {
    constructor(id, createdAt, address, total_price) {
        this.id = id
        this.createdAt = createdAt
        this.address = address
        this.total_price = total_price
    }
}